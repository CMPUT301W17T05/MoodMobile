<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <Button
        android:text="Add a New Friend"
        android:layout_height="wrap_content"
        android:id="@+id/AddNewFriendButton"
        android:layout_width="match_parent"
        android:layout_above="@+id/FilterFollowingListButton"
        android:layout_alignParentStart="true" />

    <Button
        android:text="Filter my following list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/FilterFollowingListButton"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true" />

    <ListView
        android:layout_width="match_parent"
        android:layout_height="350dp"
        android:id="@+id/FriendsListView"
        android:layout_above="@+id/AddNewFriendButton"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/NumberFriendsTextView" />

    <TextView
        android:text="You are following 0 people"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/NumberFriendsTextView"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

</RelativeLayout>